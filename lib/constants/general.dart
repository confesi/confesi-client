const bool kProductionBuild =
    true; // state only loads property (for some reason?) with this being "TRUE" (production build)
const String kDomain = "http://192.168.1.100:3000";
const int kTabletBreakpoint =
    1000; // 650 is pretty standard? not sure how good that would look though...
